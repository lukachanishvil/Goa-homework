# Ask the user for a string
user_string = input("Enter a string: ")

# Find and print the length of the string
length = len(user_string)
print("The length of the provided string is:", length)

